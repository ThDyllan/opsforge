# Local Backups

Phase 3 PostgreSQL backups are written to this directory by `scripts/backup.ps1`.

Generated backup files are local runtime artifacts and must not be committed. Only this README is tracked.
