apiVersion: v1
kind: Service
metadata:
  name: opsforge-api
  namespace: opsforge
spec:
  type: NodePort
  selector:
    app: opsforge-api
  ports:
    - name: http
      port: 8000
      targetPort: http
      nodePort: 30080
